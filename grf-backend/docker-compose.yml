version: "3.9"

services:
  grf-server:
    build: .
    ports:
      - "8765:8765"
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn server:app --host 0.0.0.0 --port 8765

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
